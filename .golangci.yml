version: "2"

formatters:
  enable:
    - gci
    - gofumpt
    - goimports
    # - golines
  settings:
    golines:
      max-len: 0
    gci:
      sections:
        - Standard
        - Default
        - Prefix(github.com/shopware)
