# Extra autostart processes
# exec-once = uwsm-app -- my-service

# set cursor theme
exec-once = hyprctl setcursor "WhiteSur cursors" 24

# librepods now runs as a supervised systemd user service (auto-restarts on crash):
#   ~/.config/systemd/user/librepods.service
