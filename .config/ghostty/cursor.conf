# Loaded after the Omarchy theme config-file so it wins: Omarchy 4.x emits
# cursor-color from bright_foreground (white), and a repo-installed theme may no
# longer ship its own ghostty.conf.
cursor-color = 81c784
